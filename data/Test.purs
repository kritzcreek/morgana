module MorganaTest where

hello = 1 + 2

whatever :: Int
whatever (Just {hello, whatever: x}) =
   do hello x x + show x
      hello x x

      rofl <- x test

      -- x
      x {hello}

      pure {x: x}
      where
        X q = let a = b in b rofl
        lol = let c = v in v asd


double :: Int -> Int -> Int
double x = x + x





asd = asdvfc

  -- (delete-all-overlays)
